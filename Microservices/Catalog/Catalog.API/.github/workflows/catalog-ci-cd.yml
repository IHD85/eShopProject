# name: Catalog Service CI/CD

# on:
#   push:
#     branches: [ master ] 
#     # VIGTIGT: Trigger kun hvis filer i Catalog Service ændres. 
#     paths:
#       - 'Microservices/Catalog/**'
#   workflow_dispatch: # Tillader manuel kørsel

# env:
#   # Navn på det Docker-image, der skal bruges i GHCR
#   DOCKER_IMAGE_NAME: eshop-catalog-service

# jobs:
#   build_and_push_docker:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write # Vigtigt: Giver adgang til at pushe til GHCR

#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       # 1. Login til GitHub Container Registry (GHCR)
#       - name: Login to GHCR
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }} # Bruger det indbyggede token fra GitHub

#       # 2. Definer Tags (Sætter 'latest' tagget)
#       - name: Define Image Tags
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ghcr.io/${{ github.repository }}/${{ env.DOCKER_IMAGE_NAME }}
#           tags: |
#             type=raw,value=latest,enable={{is_default_branch}}

#       # 3. Byg og Push Docker Image
#       - name: Build and Push Docker Image
#         uses: docker/build-push-action@v5
#         with:
#           context: . # Roden af repository'et er build konteksten
#           # BEMÆRK: Stien er justeret efter din compose.yaml
#           file: ./Microservices/Catalog/Catalog.API/Dockerfile
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}